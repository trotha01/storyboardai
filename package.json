{
  "name": "storyboard-ui-cdn",
  "private": true,
  "version": "0.0.0",
  "description": "CDN/no-install variant for Codex sandbox",
  "scripts": {
    "test": "echo \"(offline env) skipping tests\" && exit 0",
    "build": "rm -rf dist && mkdir -p dist && (cp -r public/* dist/ 2>/dev/null || cp index.html dist/index.html)"
  },
  "dependencies": {},
  "devDependencies": {}
}
